using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TcgEngine.UI;

namespace TcgEngine.Client
{
    /// <summary>
    /// 玩家攻击区域（PlayerAttackZone1）
    /// 
    /// 注意：已重命名为 BoardSlotPlayer，为了兼容更新保留此文件。
    /// 主要用途是继承/覆盖旧文件，以免在更新后丢失引用。
    /// </summary>
    public class PlayerAttackZone1 : MonoBehaviour
    {
        // 已重命名为 BoardSlotPlayer，但保留此空类以覆盖旧版本文件
    }
}